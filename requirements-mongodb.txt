# MongoDB Vector Storage Requirements
pymongo>=4.6.0
pymongo[tls]>=4.6.0

# FastAPI and dependencies (already in main requirements, listed for reference)
fastapi>=0.104.0
uvicorn>=0.24.0
pydantic>=2.0.0

# Existing dependencies maintained
pyyaml>=6.0
python-multipart>=0.0.6
